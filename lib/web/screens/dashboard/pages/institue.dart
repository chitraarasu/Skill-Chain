import 'package:flutter/material.dart';

class Institute extends StatelessWidget {
  const Institute({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Indtitute"),
    );
  }
}
