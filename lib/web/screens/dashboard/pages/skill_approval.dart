import 'package:flutter/material.dart';

class SkillApproval extends StatelessWidget {
  const SkillApproval({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Skill Approval"),
    );
  }
}
